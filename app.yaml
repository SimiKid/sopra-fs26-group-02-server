# https://cloud.google.com/appengine/docs/the-appengine-environments#comparing_high-level_features
# https://cloud.google.com/appengine/docs/standard

runtime: java
env: flex

runtime_config:
  operating_system: ubuntu22
  runtime_version: "17"

manual_scaling:
  instances: 1

env_variables:
  OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY}
  SPRING_PROFILES_ACTIVE: "prod"
  DB_URL: "jdbc:postgresql://google/weatherwizards?cloudSqlInstance=sopra-fs26-group-02-server:europe-west6:weather-wizards-db&socketFactory=com.google.cloud.sql.postgres.SocketFactory"
  DB_USER: "postgres"
  DB_PASSWORD: '-9`{m\yXir@.}dg9'